# Build Instructions

```console
cmake -S . -B build 
cmake --build build
```

# Dependencies

Add dependencies using git submodules.

```console
git submodule add <repository_url> <path/to/dependency>
```
